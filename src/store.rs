use std::path::Path;
use bawikv::BawiKv;
pub struct Store (u8)

impl Store {
    pub fn new() -> Self {

    }
    fn get(key: &str) -> String {
        "get value"
    }

    fn put(key: &str, value: &str) -> String {
        "put value"
    }

    fn find_by_key(key: &str) -> {
        "find value"
    }
}
